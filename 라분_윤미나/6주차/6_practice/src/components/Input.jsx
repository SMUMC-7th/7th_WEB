const Input = ({value, onChange}) => {

    return(
        <input
            type='text' 
            value={value} 
            onChange={onChange}
        >
            
        </input>
    );
    
};

export default Input